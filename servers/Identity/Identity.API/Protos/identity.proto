syntax = "proto3";

option csharp_namespace = "Identity.API";

package Identity;

// The greeting service definition.
service Identity {
  rpc RegisterUser (RegisterUserRequest) returns (EmptyReply);
}

message RegisterUserRequest {
  string email = 1;
  string password = 2;
}

// Empty request or response
message EmptyReply {}